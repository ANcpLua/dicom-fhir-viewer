node_modules/
dist/
coverage/
*.log
.env
.env.local
.env.*.local

sample-data/*.dcm
sample-data/*.zip
# CT_small.dcm is a public NEMA WG-04 fixture (~39 kB) used by the
# frontend dicom-pixels + ImageViewer tests; track it so CI can run
# the full suite without a separate download step.
!sample-data/CT_small.dcm

backend/data/
backend/data/*.db
backend/data/*.db-journal
backend/data/*.db-wal
backend/data/*.db-shm

.DS_Store
.idea/
.vscode/
.playwright-mcp/
.claude/
